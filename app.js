function addNewItem(){
var ListItem = document.createElement('li')
listItem.innerText = "hello";

  document.getElementsByTagName('ul');
  list.appendChild(listItem);
}

var newItem = document.getElementById('add_todo');
  add_todo.onclick = addNewItem;
